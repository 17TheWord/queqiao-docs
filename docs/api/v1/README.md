# V1 API 参考

该 `API` 文档仅适用于 `v0.2.7` 及更早版本

## Websocket Json 请求说明

```json
{
  "api": "接口名称，例如：'broadcast'",
  "data": {
    "参数名": "参数值",
    "参数名2": "参数值"
  },
  "echo": "'回声', 如果指定了 echo 字段, 那么响应包也会同时包含一个 echo 字段, 它们会有相同的值"
}
```
